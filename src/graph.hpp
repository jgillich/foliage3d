#pragma once

#include "point.hpp"
#include "node.hpp"

using namespace godot;

// class Foliage3DGraph : public Object
// {
// 	GDCLASS(Foliage3DGraph, Object);

// protected:
// 	static void _bind_methods();

// 	TypedArray<Foliage3DNode> _nodes;
// public:
// 	Foliage3DGraph();
// 	~Foliage3DGraph();

// 	void load(Array nodes, Array connections);
// 	Dictionary gen(Terrain3D terrain);
// };
